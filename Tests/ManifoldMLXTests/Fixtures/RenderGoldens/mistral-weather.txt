=== system ===
You have the following tools available:

- get_weather: Get the current weather for a location. (arguments: location) (requires: location)

When a tool can answer a question, you MUST call the tool. Never guess values (timestamps, math, files, external data) that a tool can provide. To call a tool, respond with a single JSON object of exactly this form and nothing else:

{"name": "<one of the tool names above>", "arguments": {"<argument name>": <value>}}

Use the argument names listed for the chosen tool. Do not write the call as prose or as a Python-style function call such as `tool(value)`.


You are a helpful assistant.
=== user ===
What's the weather in Paris?
=== assistant ===
[TOOL_CALLS] [{"arguments":{"location":"Paris"},"name":"get_weather"}]
=== tool ===
{"condition":"cloudy","temperature":"18C"}
=== tools (structural, threaded into applyChatTemplate) ===
[
  {
    "function" : {
      "description" : "Get the current weather for a location.",
      "name" : "get_weather",
      "parameters" : {
        "properties" : {
          "location" : {
            "description" : "The city, e.g. Paris",
            "type" : "string"
          }
        },
        "required" : [
          "location"
        ],
        "type" : "object"
      }
    },
    "type" : "function"
  }
]
